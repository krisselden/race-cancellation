{
  "name": "race-cancellation",
  "version": "0.2.0",
  "description": "a promise cancellation pattern for async/wait code.",
  "license": "BSD-2-Clause",
  "author": "Kris Selden",
  "files": [
    "dist",
    "src",
    "interfaces.d.ts"
  ],
  "main": "dist/index.umd.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && rollup -c",
    "prepare": "yarn build",
    "test": "yarn link && yarn link race-cancellation && tslint -p tsconfig.json && tsc -p tests/tsconfig.json && nyc qunit tests"
  },
  "devDependencies": {
    "@types/node": "^10.12.18",
    "@types/qunit": "^2.5.3",
    "nyc": "^13.1.0",
    "prettier": "^1.15.3",
    "qunit": "^2.9.1",
    "rollup": "^1.1.0",
    "rollup-plugin-sourcemaps": "^0.4.2",
    "tslint": "^5.12.0",
    "typescript": "^3.2.2"
  }
}
